name: release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Release workflow is paused while the public configuration is being rebuilt.'
